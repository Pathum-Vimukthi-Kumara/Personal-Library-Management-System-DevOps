# React frontend (production)
# Used during `npm run build` inside Docker.

# Leave API base empty in production so frontend uses
# relative `/api` via Nginx reverse proxy (see nginx.conf).
# If you need a fixed API host, set below to droplet IP, e.g.
# REACT_APP_API_URL=http://198.199.89.211:5001
REACT_APP_API_URL=
