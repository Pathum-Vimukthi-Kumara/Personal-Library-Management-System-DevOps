# Example React .env (copy to .env.development or .env.production)
# For local dev against Docker compose backend mapped to 5001 on host:
REACT_APP_API_URL=http://localhost:5001

# For container-to-container calls in Docker network (use in .env.production):
# REACT_APP_API_URL=http://backend:4000
